- Add prefix to device types received as packet
- Change devices::find and/or ?::match so that type is checked (HM Wired, HM BidCoS, ...)
- RPC Server nur neu starten, wenn config-Dateien geändert
- Batterie leer von Rauchmelder testen

When devices are available:
- Test blind_test

When AES is working:
- enforce $PEER.AES
- Check if wake up is working, for the HM-Sec-MDIR it works differently with AES enabled

Low priority:

- Delete peer from all devices it is linked to, when peer is reset

Today:

Questions:
- AES handshakes
- Lazy config, what is that?